# Mirrors [tool.poetry.dependencies] in pyproject.toml. Dockerfile.core installs
# this after `poetry install`, so the floors must not sit below the locked
# versions or that step would downgrade them.
requests>=2.32.4
seleniumbase>=4.18.1
cryptography>=44.0.1
colorama>=0.4.6
python-dotenv>=1.0.0
discord-webhook>=1.3.0
halo>=0.0.31
